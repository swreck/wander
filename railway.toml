[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install && npm run build && cp -r dist ../backend/public && cd ../backend && npm install && npx prisma generate && npx tsc"

[deploy]
startCommand = "cd backend && node dist/index.js"
